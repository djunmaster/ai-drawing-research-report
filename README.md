# Auto Claw · AI 驱动 CAD 电气图纸自动分析解决方案

> 从 DWG 图纸自动生成设备配置清单 / 校验互感器·电缆匹配。
> 一套以**规则与坐标提取为主、LLM 视觉为校验兜底**的工程化方案，全部结论经真实示例图纸代码验证。

📊 **可视化报告（GitHub Pages）**：https://djunmaster.github.io/ai-drawing-research-report/

---

## 两个核心需求

| 需求 | 目标 | 输出 |
|---|---|---|
| ① 设备配置清单 | 识别列头柜、进/出线回路，结合选型规则 | 标准化 Excel 配置清单 |
| ② CT·电缆校验 | 提取 CT/断路器/电缆规格，按规范校验匹配 | 异常清单 + 评估报告 |

## 核心结论

- **能 100% 确定**：柜体台账、回路数、互感器数量、监控模块型号（结构化块属性 + 块计数，零 AI）。
- **可提取+校验**：每路电流、CT/电缆型号（系统图监控子图，坐标列对齐 + 视觉交叉校验）。
- **需业务输入**：选型固定件、项目抬头、待确认柜明细。

## 关键验证（真实示例）

- DWG→DXF 必须经 **ODA File Converter**（R2018 二进制不可直接挖文字）。
- 系统图 **65 个柜体**零 AI 块属性提取成功；`二次图号` 关联系统图↔二次图。
- **图→结构化 BOM 闭环**：坐标引擎 vs LLM 视觉 7/8 一致。
- **三方核对**（示例1·301）：PDF(PaddleOCR) ↔ DWG(代码) ↔ DWG(视觉)，回路数 18、互感器 54 完全一致。

## 工具链

`ODA File Converter` · `ezdxf` · `openpyxl/pandas` · `matplotlib`(渲染) · `Claude`(视觉) · `PaddleOCR`(PP-OCRv4) · 规则依据 `GB/T 1208-2006` / `GB 50054`

---

*更新于 2026-06-30 · 基于「宁夏 D03」「301-单路进线18路出线」示例验证*
